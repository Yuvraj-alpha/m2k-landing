/** PHASE 3 STUB — real page lands in a later phase. Exists now so that
 *  `typedRoutes` can verify the header and footer links resolve. */
export default function Page() {
  return (
    <div className="mx-auto w-full max-w-5xl px-6 py-24">
      <h1 className="text-4xl font-extrabold capitalize">products</h1>
      <p className="text-muted-foreground mt-3">Coming in a later phase.</p>
    </div>
  );
}
